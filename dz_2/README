-- Реализовать на языке C/C++ классические операции перемножения квадратных матриц и умножения матрицы на вектор (3 балла) 
	файлы matrix.c, matrix_functions.c, matrix_functions.h подглядывал в Ваш код в третьей лекции

-- Разделить программу на несколько модулей и провести сборку через статическую линковку (2 балла)
	получил "о"-файлы

-- Подготовьте две сборки с флагами -g и  -O3 и измерьте времена выполнения операций с N = 512, 1024..., 4096 (3 бала)
	Сборка и время работы в файлах g.txt и O3.txt соответственно. для 4049 результатов не дождался. Результаты измерений не совсем честные т.к. замер выполнялся только один раз, а не усреднением нескольких запусков  
	
-- Измерить времена выполнения программ в виртуальной машине, реальной ОС и (опционально) в докере (3 балла + 0.5 балла за докер)
	Собрал докер, собрал исполняемый файл запустил измерения. 
	Время работы в файлах g_docker.txt и O3_docker.txt соответственно. Интересное наблюдение, что время выполнения на размере 512 и 2048 практически не отличается, а на 1024 отличается в два раза. Случайность?   
	
-- Реализовать базовые скрипты bash, перечисленные на лекции (1.5 балла)
	Скрипты находятся в папке dz_bash

-- Дополнительные баллы за вызов теста LINPACK на вашем компьютере (1.5 балла)
	Запустил тест. Результаты в linpack_test.txt
